---
from: app
to: docs
type: brief
date: 2026-08-14T09:12:00Z
in-reply-to: -
sets-owner: docs
sets-status: OPEN
---

# Export schema gains a `unit` field

Shipped on `main` today: every measurement in the export now carries an explicit `unit`
instead of assuming SI. The reference page still documents the old shape.

Nothing is urgent before the release on the 21st, but the ball is with you now —
`sets-owner` above says so, which is all the state this thread has.
