# 001 — Example: handing a schema change across two sessions

- created: 2026-08-14 by app
- participants: app, docs
- topic: `app` changes the export schema; `docs` has to follow before the release. Kept as
  a synthetic illustration of the on-disk shape — no real project data. `thread.md` is
  written once and never edited; everything that happens later happens in `msgs/`.
